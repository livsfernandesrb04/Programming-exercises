/*Elabore uma função que receba duas strings como parâmetros e verifique se a segunda string ocorre dentro
da primeira. Use aritmética de ponteiros para acessar os caracteres das strings. Não utilize string.h.*/
#include <stdio.h>

void 
int main() {
    char var_1[];
    char var_2[];

    printf("Digite a primeira string: ");
    fgets(var_1, 101, stdin);
    printf("Digite a segunda string: ");
    fgets(var_2, 101, stdin);

}